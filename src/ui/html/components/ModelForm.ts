import Context from "../../Context";
import { ModelClass } from "../../../data/Model";

type ModelFormProperties = {
    type: ModelClass
}

export default function ModelForm(c: Context, p: ModelFormProperties) {
    let { type } = p
}
