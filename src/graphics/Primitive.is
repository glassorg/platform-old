import .GL.*

//  we can promote this enum later to smart objects which help draw the right amount
export default enum Primitive
    triangles = GL.TRIANGLES
    points = GL.POINTS
    lines = GL.LINES
    models = -1

