import .INode
import ..math.Vector3

export default class PickResult

    node: INode
    position: Vector3

    constructor(node: INode, position: Vector3) ->
        this.node = node
        this.position = position
